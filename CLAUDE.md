# 硝子窓 — STAINED GLASS

大正ロマンの窓に硝子を嵌める、塗り絵の癒しアプリ。
薄暗い洋館から窓を見上げ、空いた区画をタップすると
色硝子が嵌まる。
全部埋まると教会の鐘が鳴り、窓の外へ光が差し込む。

ビルドなし。素の HTML / CSS / JS。GitHub Pages にそのまま置ける。

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # ロジックのテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 npm i -D playwright)
```

`npm run test:ui` は本物のブラウザ (iPhone の画面) を立ち上げ、指の操作をそのまま
再現する。**画面を触ったときの不具合はこちらでしか捕まらない。**
変更したら必ず両方流す。

**測る端末は 2つ** (`browser-test.js` の `PHONE` / `SMALL`)。

| | 端末 | 画面 | 役目 |
| --- | --- | --- | --- |
| `PHONE` | **iPhone 16 Plus** | 430 x 739 | 実際に遊んでいる端末。**報告する数字はこの画面のもの** |
| `SMALL` | iPhone SE | 320 x 568 | いちばん狭い画面。ここで壊れなければ、たいていの端末で壊れない |

画面まわりを直したら、**この2つで測った数字を出すこと。**
別の端末の数字で話をしない。

## 構成

```
index.html    画面・描画・音・操作。CSS も JS もこの中
core.js       ロジック。DOM を触らない。ブラウザと node の両方で動く
              (枠の割り方・窓の外形・切り抜き・点の判定)
core.test.js  ロジックのテスト (node --test)
browser-test.js  ブラウザで動かすテスト
serve.js      開発用サーバー
sw.js         Service Worker (オフライン対応)
room.jpg      タイトルの部屋の絵 (196KB)
manifest.json / *.png   PWA まわり
```

画面は4つ。**タイトル**(部屋・題の額・難易度・硝子棚・つづきから)、
**遊ぶ画面**(窓と色の帯だけ)、**硝子棚**(持っている硝子と木枠・つぎに増える物・
飾った窓)、**飾った窓を大きく見る画面**。
難易度をタイトルへ移したぶん、硝子を大きく見せる。

`window.__app` はテストから中身をのぞくための入口。`browser-test.js` が使う。
`screen()` でどちらの画面か、`cleared()` / `frame()` で棚のようすが分かる。
`redraw()` は硝子を描き直す (重さを測るのに使う)。
`setWindow` は、決まった枠をそのまま出して見比べる (画像で before/after を
並べる) ための入口。

## 決めごと

- **言葉づかいの規則 — 読んで決める言葉は日本語、銘として眺める言葉は欧文。**
  大正浪漫は純和風ではなく和洋折衷。欧文が混ざること自体はむしろ正しい。
  だめなのは配り方で、当時の看板や缶のラベルでは、欧文は**意匠**(飾り・銘)
  として使われ、人が読んで判断する言葉は日本語だった。
  - 日本語 … のこり枚数・ボタン・知らせ・硝子と木枠の名・難易度の枚数
  - 欧文 … 窓と枠の**銘**。漢字の和名を大きく、その下に字間をあけた
    小さな欧文を添える二枚組で出す (`WINDOW_SHAPES[].jp` / `HANDMADE[].jp`)。
    アイコンの中の `STAINED GLASS` も看板の欧文なので残す
  - **例外は難易度だけ。** 規則どおりなら日本語だが、和語に直すと
    (ひと息・ゆるり…) 語感が合わなかったので欧文に戻した
    (very easy / easy / normal / hard / very hard)。添え書きの枚数は日本語。
    **規則は道具であって主人ではない。** 当てはめた結果が気持ち悪ければ、
    規則ではなく現物を採る。ただし、そう決めたことはここに書き残す
  - `name` は保存にも使う見出しなので欧文のまま。表に出すのは `jp`
  - テストで見張る (核: 和名と欧文が両方そろっているか。画面: 難易度が
    欧文で枚数が日本語か・銘が二枚組か・遊ぶ画面に英語の取り残しが無いか)

- **難易度は5段** — very easy 10枚 / easy 20 / normal 40 / hard 80 /
  very hard 140 (`DIFF_ORDER`)。押すたびに窓の外形 (縦長・四角・アーチ・
  ゴシック・丸窓) を抽選し、左右対称か非対称のランダム分割で枠を作る。
  35% の確率で手作りの幾何学枠15種が混ざる
- **段の間隔をそろえる。** 2.0 / 2.0 / 2.0 / 1.75倍。10→40 の4倍だけが
  崖になっていて、easy が気持ちよかった人の降りる場所が無かった。
  いちばん広い段がいちばん狭い段の1.5倍を超えないことをテストで見張る。
  140 が天井 (それ以上は指で押せる大きさのセルに割れない。実際の中央値も
  133枚どまり)
- **実在の文様を、敷き詰めで作る** (麻の葉・亀甲・籠目・八角つなぎ・立涌)。
  ランダム枠は「分割」(適当に切る)、実在の文様は「敷き詰め」(同じ形が
  噛み合って繰り返す)。後者には律動がある。大正浪漫は和洋折衷なので、
  洋館の窓に組子の文様を硝子で組むのは、まさにあの時代の姿。
  立涌だけは曲線 (折れ線で近づける)。他は全部直線
- **敷き詰めは、窓の幅と高さで割り切る。** 半端に切ると縁に 17px の欠けらが
  残り、指で押せない。列数を決めてから幅を `1/列数` にし、段も同じように割る
  (画面の上で正三角・正六角になるよう `PANEL_R` で補正する)。
  麻の葉は、縁にかかる三角だけ重心で割らず丸ごと残す (割ると破片になる。
  本物の窓も、文様は枠でそのまま切られている)。八角つなぎは窓のふちで
  角を落とさずぴたりと付ける。立涌はいちばん外の2本をまっすぐにする
  (どちらも、そうしないと縁に隙間が出る)
- **敷き詰めの文様は、出せる細かさが飛び飛び。** 押せる大きさを守れる刻みしか
  使えないので、全部の段には出ない (`closestTo` の `keep` に `allTappable` を
  渡して、割り切れる細かさだけ選ぶ)。

  | 文様 | 枚数 | 出る段 |
  | --- | --- | --- |
  | 麻の葉 | 44 / 102 / 161 | normal 〜 very hard |
  | 亀甲 | 10 / 18 / 45 | very easy 〜 normal |
  | 籠目 | 32 / 66 | normal・hard (いちばん狭い) |
  | 八角つなぎ | 8 / 23 / 39 / 68 / 137 | **全5段** |
  | 立涌 | 12 / 20 / 30 / 63 | very easy 〜 hard |

  **どの段にも、実在の文様が二つ以上出ること。** テストで見張る
- **同じ難易度なら、同じ手応え。** 手作り枠も目標枚数を受け取って
  細かさを変える (細かさを何通りか作り、目標にいちばん近いものを選ぶ)。
  どの枠をどの難易度に混ぜてよいかは `fitsDifficulty` が決める条件
  (枚数が目標の±35%以内・どのセルも指で押せる大きさ) で、表と
  食い違っていないかをテストで照らす
- **帯から外れた窓は引き直す** (`buildInBand`)。`fitsDifficulty` は手作り枠
  にしかかかっていなかったので、ランダム枠は帯の外へ出られた。出来上がり
  (切り抜きとかけら預けまで済ませた押す枚数) を見て、外れていたら引き直す。
  8回引いても収まらなければ、いちばん目標に近かった窓を出す
- **セルの「中の一点」は、いちばん辺から遠い所を選ぶ** (`insidePoint`)。
  三日月のような形は外枠が大きくてもどこも細く、重心が外へ出たり、
  ふちすれすれの点になったりする。そこを狙って押すと隣のセルに入る。
  候補をいくつも出して、辺からの距離がいちばん大きい点を採る。
  高いので、セルが変わった時に一度だけ計算して使い回す (`mids`)
- **「押せる大きさ」は外枠では測れない。** 指の腹ぶんの円 (半径 4.5px) が
  入るかどうかで見る (`attachSlivers`)。入らないセルは、外枠が大きくても
  隣に預ける
- **塗りかけは消さない。** 窓と嵌めた硝子は localStorage に覚えて、
  開き直したら続きから。塗りかけのまま引き直す時は一度だけ聞く。
  段を足して番号がずれた時も、古い保存 (v1 の easy=10枚) は読み替えて
  引き継ぐ (`DIFF_ALIAS_V1`)
- **なぞって塗れる。** 窓の中から始めたら、なぞった道すじを塗る
  (すでに嵌まっている硝子は替えない)。窓の外から速くはらうと光の帯。
  道すじは 3px ごとに拾う。約束は「6px 以上またいだセルは必ず塗れる」
  (角をかすめただけの所までは求めない)
- **指で押せない大きさのセルを作らない。** 割る時は難易度ごとの下限
  (19〜26px) を守る。外形で切り抜いた時に出るかけらは、隣の押せる
  セルに預けて一緒に嵌める (`attachSlivers`)
- **外周に縁取りの帯。** normal 以上では 7 割の確率で、外形に沿った帯を
  めぐらせてから内側を割る (`makeWindow`)。帯の幅は短いほうの辺の1割、
  帯幅の3倍の長さで割る。内側に回す枚数は目標から帯のぶんを引くので、
  枚数は縁なしと変わらない。小さい画面の四角窓・丸窓など、帯を付けると
  枚数が2割以上落ちる時は帯をあきらめる
- **鉛線は弧に曲げる。** 割り方は変えず、出来上がった鉛線をあとから
  膨らませる (`conformCells` → `curveLeading`)。隣り合う2枚が同じ弧を
  共有するので、隙間も重なりも出ない。まず T 字の辺をそろえてから曲げる。
  細いセル・帯・手作り枠は曲げない。弧が他の辺を突き抜ける時は入れない
- **窓ごとに「表情」を引く** (`CURVE_STYLES`) — 直線だけ 20% / ほどほど 45% /
  曲線多め 35%。手作り枠 (窓の35%) はいつも直線なので、全体では
  直線だけ 48% / ほどほど 29% / 曲線多め 23% になる
- **曲げたあと、出来上がりを検める。** 面積の合計は曲げても変わらないはず
  (弧は隣どうしで分け合う)。合っていなければ重なっているので、弧を1本ずつ
  外す。左右対称の窓で対称が崩れた時は、その窓は曲げずに返す
- **窓の銘は、上に二段で出す。** 銘が二段になったぶん、上の余白は 44px
  (`TITLE_SPACE`)。銘が窓にかぶらないこと、もどる矢印とのこり枚数に
  ぶつからないことをテストで見張る (いちばん広い銘は菱格子 135px。
  iPhone SE でものこり枚数との間が 77px あく)
- **遊ぶ画面には、窓と色だけ。** 難易度はタイトルで選ぶ。
  遊ぶ画面の左上に、小さく「もどる」
- **地は、一枚の絵** (`room.jpg` — `#room`)。洋燈・硝子窓・
  椿・書棚・床の光だまりまで写った洋館の一室。**ここだけは手で描かない。**
  はじめは canvas に描いていた (壁・腰板の目地・灯り・床の光・毎回ちがう窓)
  が、絵にあった小物までは手で描ける形ではなかった。絵を1枚もらえるなら、
  そちらのほうが強い。`object-fit: cover` で画面いっぱいに切り取り、
  窓が絵の右手にあるので `object-position: 62% 34%` でそちらを残す。
  重さは 196KB (JPEG q0.85 / 1086x1448)。`sw.js` の先読みに入れて
  オフラインでも出す。**タイトルも遊ぶ画面も同じ一枚**で、遊ぶ画面では
  暗幕を濃くして (`#room-veil.playing`) 硝子窓のほうを立てる
- **絵の上に薄い暗幕** (`#room-veil`)。上に丸く、下へ向かって濃く。
  これが無いと、床の光だまりの上で品書きの字が読めない
- **canvas は絵を透かす。** 地を描かず `clearRect` する (`drawBackground`)。
  **タイトルのあいだは何も描かない** —— 描いたままだと、遊んでいた窓と
  木枠が題の裏に透けて出る。テストで見張る (タイトルで canvas が白紙か)
- **重なりの順は一か所で決める。** 絵 0 / canvas 1 / 色の帯・見出し・
  もどる 3 / タイトル 5 / 確認 6 / 硝子棚 7 / 大きく見る 8。
  canvas を `position: fixed` にした時、帯より上に来て**硝子が選べなく
  なった**。色の丸に指が届くことをテストで見張る
- **絵の上に置く札は、透かさない。** 暗い下地
  (`rgba(13,10,7,0.92)`) を敷いてから段の色を 0.30 で重ねる。
  透かしただけだと、窓の明るい所でコントラストが **1.40** まで落ちていた
  (「のこり N 枚」がほとんど見えない)。いまはいちばん薄い所で 4.90。
  **字の乗る所の画素を実際に測ってテストで見張る** (飾りの硝子の花や
  金の紋は明るくて当たり前なので、`.names` / `.note` など字の矩形だけを見る)
- その上に DOM で、題の額 (`#start-plate` — 漢字の**硝子窓**を大きく、
  下に字間をあけた `STAINED GLASS`)、そえ書き、「硝子棚」のボタン、
  難易度5段の品書き。塗りかけがあれば先頭に「つづきから」
- **品書きの一段ごとに、硝子の花を描く** (`paintRosette`)。8弁の花を
  乳白の地に鉛線で囲む。段の色 (`TIERS[].fam`) は硝子の色から採る
- **横向きのタイトルは、左右2段組み。** 縦に積んだままだと 739x430 で
  459px 必要になり、いちばん下の very hard が画面の外へ出ていた。
  題を左 (`#start-left`)、品書きを右に分ける
- **窓は入るだけ大きく。** 見出しと操作帯を除いた場所に、幅と高さの入るほうで
  最大まで広げる (木枠が外へはみ出すぶんも勘定に入れる)。横向きのときは
  操作帯を右端に移して、縦の場所を窓に譲る
- **遊ぶ画面の見出し** (`#hud`) —— 金の罫に硝子の蓮を挟み、その下ひとところに
  「窓の銘」(引いた直後 2.4秒) と「のこり枚数」(ふだん) を**重ねて**出す。
  同じ場所で入れ替わるので、ぶつかりようがない。見出しのぶん、窓の上の
  余白は 64px (`TITLE_SPACE`。44px から増やした代は窓の高さ 13px)
- **木枠は、面取りの輪を重ねて作る** (`buildFrame`)。太い線を3本重ねた
  だけの頃は、板を貼った縁にしか見えなかった。本物の窓枠は
  「外の縁 → 面取り → 溝 → 面取り → 内の縁」と段になっていて、
  その段ごとの陰影が額縁らしさを作る。輪は**外側の幅で描いてから
  内側の幅で抜く** (`destination-out`)。外から内へ順に置けば、抜いた所には
  まだ何も無いので下の輪を削らない。金の象嵌を1本と、四方に金の座金。
  壁に落ちる影も付ける (これがあって初めて「掛かっている」ように見える)。
  canvas に焼いておき、毎コマは貼るだけ
- **木枠を描く外形だけ、弧の折れ目を丸める** (`smoothOutline` → `framePath`)。
  外形は折れ線なので、半円窓や薔薇窓の弧は多角形。**割り方 (セル) の側は
  触らない** —— 点を増やしたら縁取りの帯が壊れた。30度より大きく曲がる所は
  「角」として残し、ゆるい所だけ角切りする
- **硝子には練りむら** (`mottlePattern`)。本物の色硝子は濃淡が流れている。
  べた塗りだと色紙を貼ったように見える。なだらかな濃淡を1枚作っておいて
  `soft-light` で重ねる (作るのは一度きり)。棚の小さな絵では潰れるので
  入れない (`box.w >= 140` の時だけ)。
  代は描き直しの時間 —— CPU 4倍おそい端末・vhard 148枚で
  2.4ms → 4.2ms (1コマ 16.7ms に収まる)。テストで見張る
- **壁はひとつ。薄暗い洋館だけ。** 白い壁の版もいちど作ったが、**やめた。**
  硝子の色がいちばん映えるのは暗い部屋で、そのために光の強さも空きセルの色も
  作り分けが要り、画面の数だけ気を配る先が増えていた。
  「どちらでもよい」ものを二つ持つより、狙いの一つを深くする。
  色は canvas も CSS も `theme` 一か所から配る
  (CSS は `--ink` などのカスタムプロパティ経由で `applyWall` が流し込む)。
  `<meta name="theme-color">` と manifest も同じ色 (`#14110e`) に
  そろえる (iOS のステータスバーの地になる)
- **硝子は、その色の明るさに応じて光らせる。** どの色にも同じだけ白を足すと、
  深い藍も深い臙脂も中間色になり、同じ系統の濃淡も見分けがつかなくなる
- **硝子棚 — 遊ぶほど増える。減らさない。** 窓を1つ埋めきるごとに1つ数え、
  硝子の色 (10→20系統) と木枠の塗り (1→5種) が増えていく
  (`COLOR_FAMILIES[].at` / `FRAMES[].at`)。難易度で重みは付けない。
  最初から持っている10色は今までのまま (持っていた物は取り上げない)。
  増えた時は鐘のあとに一度だけ知らせる。記録は localStorage
  (`stained-glass:progress`)。まだ手に入れていない木枠がしまってあったら、
  最初の木枠に戻す
- **仕上げた窓は、棚に飾る。上限 20枚** (`SHELF_MAX`)。窓を埋めきると自動で
  飾られる。いっぱいなら**いちばん古い窓と入れ替え**、黙って消さずに画面で知らせる。
  1枚ずつ押すと大きく見られて、そこから捨てられる。
  記録は硝子棚とは別の引き出し (`stained-glass:shelf`) —— 片方が壊れても、
  もう一方は無事でいられるように
- **飾る時は、しまう形に通してから戻す。** そうしないと、飾った窓が遊んでいる窓と
  同じ配列を指したままになり、仕上げたあとに色を差し替えると飾りも変わってしまう
- **重さは測ってある。** 1枚は veasy 4KB 〜 vhard 24KB。20枚でも最大 0.5MB で、
  localStorage の目安 5MB に十分おさまる。それでも書けない端末はありうるので、
  断られたら古いほうから1枚ずつあきらめて書き直す (`saveShelf`)
- **硝子は一か所で描く** (`paintGlass`)。遊ぶ画面の窓も、棚に並ぶ小さな絵も、
  大きく見る絵も、同じ手で描く。描く場所と鉛線の太さだけを渡す
- **棚では、まだの物も灰色で見せる。** 「あと何窓か」を添える。
  何が待っているか見えるほうが、次の窓を引く手が動く
- **色は二段に並べる。持っている色は、全部見えている。** 横になぞって出す
  形にしたら、選ぶのが手間だった (10色でも2個は画面の外にいた)。
  一段あたりの数は「色の数の半分」。そこから画面の幅に入る大きさを出す
  (`layoutSwatches`)。押せる所は 44px (Apple の目安) を上限に、詰まって
  きたら小さくする。**32px は割らない — 割るくらいなら段を増やす。**
  横向きでは帯が右端に立つので、幅ではなく高さで同じことをする
- **色の丸は、見た目と押せる所を分ける。** 見える丸は「押せる所 − 8px」。
  前は丸が 26px しかなく、押しにくかった。画面のいちばん下はホームバーを
  上げる指の通り道なので、帯は 18px + セーフエリアぶん上げる
- **二段にした代は、窓の高さ。** 帯は 70 → 114px。全部が見えて、なぞらずに
  押せることを取った。iPhone 16 Plus では 10色・16色とも二段 44px のままで、
  20色そろうと 41px まで詰まる (iPhone SE だけは 20色で三段 43px になる)
- **文様の名も和名で。** 麻の葉 (Hemp Leaf) ・ 亀甲 (Tortoiseshell) ・
  籠目 (Basket Weave) ・ 八角つなぎ (Linked Octagons) ・ 立涌 (Rising Steam)
- **色名は大正の頃の色名から。** 紅・臙脂・琥珀・黄檗・常磐・群青・藤・
  鶯・鉄紺・葡萄・煤竹…。木枠は 黒檀・古色・朱塗・白木・青銅。
  窓は 竪長窓・角窓・半円窓・尖塔窓・薔薇窓、手作り枠は 麻の葉・亀甲・
  籠目・八角つなぎ・立涌・市松・大菱・三つ菱・光芒・列柱・煉瓦・扉板・
  菱格子・車輪窓・山形
- **音は合成する。** 音源ファイルは持たない。硝子の「チン」は非整数倍音、
  鐘はハム・プライム・短三度…を積む
- **正しさは仕組みで保証する。** 枠は「隙間なく・重なりなく・全部押せる」が
  成り立つように作り、それをテストで見張る

## 踏んだ落とし穴

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 直したのに古い画面が出る | サービスワーカーがキャッシュ優先だった | 画面とコードは network-first。絵だけキャッシュ優先 |
| 最後の1枚が埋まらず鐘が鳴らない | 外形で切り抜いた時、ふちに 8px のかけらが残っていた。割る側の最小サイズは守られていても、切り抜きがそれを壊す | 押せない大きさのかけらは、いちばん近い押せるセルに預けて一緒に嵌める |
| ホーム画面のアイコンが違うものになる | iOS は `apple-touch-icon` に SVG を使えない | PNG を用意する |
| 押しても反応しないセルがある | 操作帯に足した確認欄に `display:flex` を書いたら、ID 指定が `[hidden]{display:none}` に勝って常に出たままになり、窓の下をふさいでいた | `#ask[hidden]{display:none}` を書く。操作帯が窓にかぶらないことをテストで見張る |
| 同じ難易度なのに、終わるまでの手数が4倍違う | 手作り枠の枚数が固定 (12〜28) で、目標 40 のランダム枠と混ざっていた | 枠の側も目標枚数を受け取って細かさを変える |
| 窓の外形に沿って帯を作れない | 外形に同じ点が2つ並んでいて (直線から弧に移るところ)、辺の長さが 0 になっていた | 外形は `cleanPoly` を通してから使う。重なった点が無いことをテストで見張る |
| 曲げたら硝子が窓の外へはみ出した | 窓のふちの辺まで曲げていた。切り抜きで出た細片は同じ辺を2回持つので、「別々の2枚が共有している辺」に見えていた | 共有していても、別のセルどうしか確かめる |
| 弧を入れたらセルの形が壊れ、2枚が重なった | 細長い三日月のようなセルで、弧が向かいの辺を突き抜けていた。太さを bbox で見ていたので気づけない | 弧が他の辺と交わらないか、実際に確かめてから入れる。同じセルに2本入ると弧どうしが交わるので、出来上がりも検めて壊れていたら弧を外す |
| 外形のふちに、埋められない穴が残る | 切り抜きで出た小片を捨てすぎていた (縁から 31px の深さまで抜けていた。木枠が隠すのは 15px) | 捨てる基準を下げる。小さなかけらは隣のセルに預けて一緒に嵌まる |
| テストの「左右対称」がときどき落ちる | 重心を丸めた文字列で突き合わせていて、0.65625 が 0.6562 と 0.6563 に分かれていた | 丸めずに、いちばん近い重心と1対1で対応づけて許容差で見る |
| 同じ easy なのに、8枚の窓と18枚の窓が出る | 「目標の±35%」の見張りが手作り枠にしかかかっていなかった。ランダム枠は素通りで帯の外へ出られた。許容幅の下限 4枚 も、目標10 では ±40% になっていた | 出来上がりの押す枚数で見て、帯から外れたら引き直す (`buildInBand`)。許容幅はどの段も同じ割合にする |
| 押しても隣のセルが嵌まる硝子がある | 三日月形のセルで「中の一点」がふちから 0.4px の所にあり、指の座標が1px 丸められると隣へ逃げていた。外枠は 40×69px もあるので、大きさの見張りには引っかからない | 中の一点は、候補の中で辺からいちばん遠い所を選ぶ。「押せる大きさ」も外枠でなく、指の腹ぶんの円が入るかで見る |
| なぞった道すじに塗り残しが出る | なぞりを 6px ごとにしか拾っていなかった。斜めに掠めたセルは、またいだ長さが 6px に満たず飛ばされる (51回なぞって7枚) | 3px ごとに拾う (塗り残しは「1.5px 未満しかまたいでいない所」だけになった)。CPU 4倍遅い端末・vhard でも、なぞり1コマ 2.4ms |
| 「つづきから のこり N」が、遊ぶ画面の枚数と1枚ちがう | タイトルでは色の帯が隠れていて高さ 0 として測るので、窓が大きく組まれ、かけらの預け先が変わっていた | 帯が隠れている時も、いったん見えない形で出して測って覚える。どちらの画面でも窓の大きさが同じことをテストで見張る |
| 開き直すと、嵌めた硝子が 5枚から 21枚に**増えて**いた (8回に1回ほど) | 起動の順が `restoreSaved()` → `rebuildSwatches()` だったので、**色の丸がまだ1つも無い空の帯**を測って窓を戻していた。板が本当より高く出て、かけらの預け先が変わる。`refreshHosts` は「同じ持ち主のセルは同じ硝子にそろえる」ので、預け先が変わったぶん、嵌めてある硝子が隣へ**写って広がった**。あとで `resize()` が預け先を測り直しても、広がった硝子はもう戻らない | **並べる → 測る → 戻す**の順にする (`rebuildSwatches()` → `resize()` → `restoreSaved()`)。開き直しても嵌めた枚数と預け先が変わらないことをテストで見張る |
| 大正浪漫のはずが、どっち付かずに見える | 混ざっていることではなく、**逆さま**なのが原因だった。難易度やのこり枚数という「読んで決める言葉」が英語で、硝子の色名という「味わう言葉」が日本語。当時の看板とは逆 | 規則を一本引く ——「読んで決める言葉は日本語、銘として眺める言葉は欧文」。以後はこの規則に照らして決める |
| 実在の文様を入れたら、縁に押せない欠けらが残る | 格子を窓に敷いて縁で切ったので、半端な破片 (17px) が出た。三角を重心で3つに割る麻の葉では、さらに細かく砕けた | 格子を窓の幅・高さで割り切る (幅 = 1/列数)。縁にかかる三角は割らずに丸ごと残す。押せる細かさだけ選ぶ門番 (`allTappable`) を通す |
| 色を横になぞって選ぶのが手間 | 一段に並べて入りきらないぶんを横スクロールにしたので、10色でも2個、20色なら11個が画面の外にいた | 二段に並べて全部見せる。一段の数は色数の半分、丸の大きさは画面の幅から決める |
| 色を選ぶ丸が押しにくい | 丸が 26px しかなく (Apple の目安 44pt の6割)、画面のいちばん下から 10px の所にあった。そこはホームバーを上げる指の通り道 | 見える丸 34px・押せる所 44px に分け、帯を下から離す。大きさと位置をテストで見張る |
| 嵌めた硝子が、どれも淡く似た色に見える | 色の明るさに関わらず一律に白い光を足していた (濃い色ほど持ち上がり、平均 +22 L*) | 光の強さをその色の明るさで決める (+6 L* に収まり、濃淡も見分けられる) |
| タイトルの木枠が「硝子棚」の字に乗る | 窓を画面幅から決めて右へ置いただけで、前景がどこまで来ているかを見ていなかった | 前景の右端を測って、その右へ寄せる。ただし**高さの重なる物だけ**を見る (縦向きの品書きは窓より下にあるので勘定に入れない) |
| 横向きのタイトルで very hard が押せない | 縦に積んだままだったので、739x430 の画面に 459px 必要だった。`overflow: hidden` なので、はみ出したぶんは黙って切られる | 横向きは題を左・品書きを右の2段組みにする。画面に収まることをテストで見張る |
| 大きい画面でタイトルの窓が色の塊に見える | 窓の幅を「画面幅の半分」にしていたので、1280px の画面では 640px になり、硝子1枚が拳ほどになった | 340px で頭を打たせる |
| 絵を敷いたら、品書きの字が読めなくなった | 札を段の色で透かしていただけなので、窓の明るい所 (rgb 236,207,127) が抜けてきて、コントラストが **1.40** まで落ちた。**暗い部屋を自分で描いていた頃は地が暗いと決まっていたので、透かしで足りていた** | 札に暗い下地を敷いてから段の色を重ねる。字の乗る所の画素を実際に測って見張る |
| コントラストを測ったら、直したのに数字が動かない | 札の全面を格子で拾っていたので、いちばん明るい点が**硝子の花の乳白の地**や**金の紋**だった。そこに字は乗らない | 字の矩形 (`.names` / `.note` / `#start-title` …) の中だけを測る |
| タイトルの裏に、遊んでいた窓が透けて出る | canvas に地を描くのをやめて絵を透かしたら、**タイトルのあいだも窓と木枠を描き続けていた**のが見えるようになった。前は canvas 自身が不透明な壁だったので気づけない | タイトルのあいだは `clearRect` だけして帰る。タイトルで canvas が白紙かをテストで見張る |
| 硝子の色が選べない | canvas を `position: fixed; z-index: 1` にしたら、`z-index: auto` の色の帯より上に来て、丸を全部ふさいだ。テストは「タップが 30秒で時間切れ」という落ち方をするので、原因が読み取れない | 重なりの順を一か所にまとめて書く。**色の丸の真ん中で `elementFromPoint` を引いて、丸が返ることをテストで見張る** |
| 外形をなめらかにしたら、縁取りの帯が壊れた | 弧の点を 28 → 48 に増やしたら、`insetConvex` で作る帯が潰れて面積が 0.2579 になった (本来 0.85)。角が細かいほど、内側へ寄せた形が自分自身と交わる | **割り方の側は触らない。**描く時だけ角切りしてなめらかにする |
| コントラストを測ったら、地を敷いたボタンが真っ暗に出た | 字を消すのに `visibility: hidden` を使っていたので、ボタンの地ごと消えて、その下の絵を測っていた | 地のある物は `color: transparent` で字だけ透かす |
| ボタンの飾りが消える | `button.textContent = "…"` で字を書き換えたら、中の SVG ごと消えた | 字は `<span>` に入れて、そこだけ書き換える |
| 白い壁の版が、直すたびに足を引っぱる | 空きセルの色・光の強さ・題の額の地……**壁ごとに作り分けが要る所が、画面を足すたびに増えていった。** どちらも「まあまあ」になり、暗い部屋の良さも薄まっていた | 白い壁の版はやめる。**選べることそのものは値打ちではない。** 狙いが一つなら、そこへ全部を寄せられる |

**新しく踏んだら、この表に足すこと。**
