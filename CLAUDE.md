# ステンドグラス

大正ロマンの窓に硝子を嵌める、塗り絵の癒しアプリ。
薄暗い洋館の中から窓を見上げ、空いた区画をタップすると色硝子が嵌まる。
全部埋まると教会の鐘が鳴り、窓の外へ光が差し込む。

ビルドなし。素の HTML / CSS / JS。GitHub Pages にそのまま置ける。

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # ロジックのテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 npm i -D playwright)
```

`npm run test:ui` は本物のブラウザ (iPhone の画面) を立ち上げ、指の操作をそのまま
再現する。**画面を触ったときの不具合はこちらでしか捕まらない。**
変更したら必ず両方流す。

## 構成

```
index.html    画面・描画・音・操作。CSS も JS もこの中
core.js       ロジック。DOM を触らない。ブラウザと node の両方で動く
              (枠の割り方・窓の外形・切り抜き・点の判定)
core.test.js  ロジックのテスト (node --test)
browser-test.js  ブラウザで動かすテスト
serve.js      開発用サーバー
sw.js         Service Worker (オフライン対応)
manifest.json / *.png   PWA まわり
```

`window.__app` はテストから中身をのぞくための入口。`browser-test.js` が使う。

## 決めごと

- **難易度は4段** — easy 10枚 / normal 40 / hard 80 / very hard 140。
  押すたびに窓の外形 (縦長・四角・アーチ・ゴシック・丸窓) を抽選し、
  左右対称か非対称のランダム分割で枠を作る。35% の確率で手作りの
  幾何学枠10種が混ざる
- **同じ難易度なら、同じ手応え。** 手作り枠も目標枚数を受け取って
  細かさを変える (細かさを何通りか作り、目標にいちばん近いものを選ぶ)。
  どの枠をどの難易度に混ぜてよいかは `fitsDifficulty` が決める条件
  (枚数が目標の±35%以内・どのセルも指で押せる大きさ) で、表と
  食い違っていないかをテストで照らす
- **塗りかけは消さない。** 窓と嵌めた硝子は localStorage に覚えて、
  開き直したら続きから。塗りかけのまま引き直す時は一度だけ聞く
- **なぞって塗れる。** 窓の中から始めたら、なぞった道すじを塗る
  (すでに嵌まっている硝子は替えない)。窓の外から速くはらうと光の帯
- **指で押せない大きさのセルを作らない。** 割る時は難易度ごとの下限
  (19〜26px) を守る。外形で切り抜いた時に出るかけらは、隣の押せる
  セルに預けて一緒に嵌める (`attachSlivers`)
- **窓は入るだけ大きく。** 題と操作帯を除いた場所に、幅と高さの入るほうで
  最大まで広げる (木枠が外へはみ出すぶんも勘定に入れる)。横向きのときは
  操作帯を右端に移して、縦の場所を窓に譲る
- **硝子は、その色の明るさに応じて光らせる。** どの色にも同じだけ白を足すと、
  深い藍も深い臙脂も中間色になり、同じ系統の濃淡も見分けがつかなくなる
- **音は合成する。** 音源ファイルは持たない。硝子の「チン」は非整数倍音、
  鐘はハム・プライム・短三度…を積む
- **正しさは仕組みで保証する。** 枠は「隙間なく・重なりなく・全部押せる」が
  成り立つように作り、それをテストで見張る

## 踏んだ落とし穴

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 直したのに古い画面が出る | サービスワーカーがキャッシュ優先だった | 画面とコードは network-first。絵だけキャッシュ優先 |
| 最後の1枚が埋まらず鐘が鳴らない | 外形で切り抜いた時、ふちに 8px のかけらが残っていた。割る側の最小サイズは守られていても、切り抜きがそれを壊す | 押せない大きさのかけらは、いちばん近い押せるセルに預けて一緒に嵌める |
| ホーム画面のアイコンが違うものになる | iOS は `apple-touch-icon` に SVG を使えない | PNG を用意する |
| 押しても反応しないセルがある | 操作帯に足した確認欄に `display:flex` を書いたら、ID 指定が `[hidden]{display:none}` に勝って常に出たままになり、窓の下をふさいでいた | `#ask[hidden]{display:none}` を書く。操作帯が窓にかぶらないことをテストで見張る |
| 同じ難易度なのに、終わるまでの手数が4倍違う | 手作り枠の枚数が固定 (12〜28) で、目標 40 のランダム枠と混ざっていた | 枠の側も目標枚数を受け取って細かさを変える |
| 嵌めた硝子が、どれも淡く似た色に見える | 色の明るさに関わらず一律に白い光を足していた (濃い色ほど持ち上がり、平均 +22 L*) | 光の強さをその色の明るさで決める (+6 L* に収まり、濃淡も見分けられる) |

**新しく踏んだら、この表に足すこと。**
